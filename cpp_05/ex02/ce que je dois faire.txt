Très bien 👍 Voici la **marche à suivre complète**, étape par étape, **sans aucun code**, pour que tu saches exactement quoi faire pour réaliser correctement l’exercice 02.

---

## 🎯 Objectif général

Tu dois :

* Transformer ta classe `Form` précédente en **classe abstraite `AForm`**.
* Créer **trois classes dérivées concrètes** :

  * `ShrubberyCreationForm`
  * `RobotomyRequestForm`
  * `PresidentialPardonForm`
* Gérer la logique d’exécution de ces formulaires par des **Bureaucrates**, avec des vérifications de grade et de signature.

---

## 🧱 1. Préparer ton projet

* Crée un dossier `ex02/`
* Mets à l’intérieur les fichiers demandés :
  `Makefile`, `main.cpp`, `Bureaucrat` (hpp/cpp), `AForm` (hpp/cpp), et les trois formulaires dérivés.
* Reprends ton code de `ex01` comme base.

---

## ⚙️ 2. Transformer `Form` en `AForm`

* Renomme `Form` en `AForm`.
* Rends cette classe **abstraite** (donc non instanciable).
* Les attributs doivent **rester privés** dans cette classe.
* C’est dans `AForm` que sont stockés : le nom, l’état de signature, le grade nécessaire pour signer et celui pour exécuter.
* Les classes filles ne doivent pas directement manipuler ces attributs, seulement utiliser les fonctions publiques d’accès.

---

## 📜 3. Ajouter le mécanisme d’exécution

* Ajoute une fonction virtuelle pure `execute` dans `AForm`.
* Cette fonction sera **redéfinie dans les classes filles**.
* Dans `AForm`, implémente une fonction utilitaire interne (ou protégée) qui vérifie :

  1. Que le formulaire est signé.
  2. Que le bureaucrate a un grade suffisant pour exécuter.
* Si ces conditions ne sont pas remplies, lève une exception.

---

## 🪴 4. Créer les classes concrètes

Chaque classe dérive de `AForm` et représente une action spécifique.
Elles doivent toutes avoir un **constructeur prenant une seule string** comme paramètre (la "cible").

### ShrubberyCreationForm

* Grades requis : 145 pour signer, 137 pour exécuter.
* Action : créer un fichier nommé avec le target + `_shrubbery`, contenant un dessin d’arbre en ASCII.

### RobotomyRequestForm

* Grades requis : 72 pour signer, 45 pour exécuter.
* Action : afficher un bruit de perceuse, puis annoncer que la robotomisation du target a réussi ou échoué, avec 50 % de chance.

### PresidentialPardonForm

* Grades requis : 25 pour signer, 5 pour exécuter.
* Action : annoncer que la cible a été graciée par Zaphod Beeblebrox.

---

## 🧍 5. Modifier la classe Bureaucrat

* Ajoute une fonction `executeForm` dans `Bureaucrat`.
* Cette fonction doit tenter d’exécuter un formulaire et afficher un message :

  * Si tout se passe bien : indique que le bureaucrate a exécuté le formulaire.
  * Sinon : affiche une erreur avec la raison (exception attrapée).

---

## 🧪 6. Tester dans le main

* Crée plusieurs bureaucrates avec différents grades.
* Crée un formulaire de chaque type.
* Teste les cas suivants :

  1. Un bureaucrate trop bas en grade essaye de signer ou d’exécuter.
  2. Un bureaucrate assez haut signe et exécute avec succès.
  3. Cas de formulaire non signé (doit lancer une exception à l’exécution).
  4. Robotomy qui réussit et qui échoue (en exécutant plusieurs fois).
  5. Vérifie que le fichier du Shrubbery est bien créé.

---

## 🧰 7. Makefile

* Crée un Makefile simple avec les règles : `all`, `clean`, `fclean`, `re`.
* Le binaire doit porter un nom clair, comme `bureaucrat`.
* Compile avec les flags standards (`-Wall -Wextra -Werror -std=c++98`).

---

## ✅ 8. Résumé de la logique

| Élément                    | Type      | Rôle principal                           |
| -------------------------- | --------- | ---------------------------------------- |
| **AForm**                  | Abstraite | Gère signature et validation des grades  |
| **ShrubberyCreationForm**  | Concrète  | Crée un fichier avec un arbre ASCII      |
| **RobotomyRequestForm**    | Concrète  | Simule une opération robotique aléatoire |
| **PresidentialPardonForm** | Concrète  | Affiche un message de pardon             |
| **Bureaucrat**             | Concrète  | Peut signer et exécuter des formulaires  |
| **main.cpp**               | Test      | Vérifie toutes les fonctionnalités       |

---

## 🧭 9. Ordre logique de développement

1. Copier ton `Form` et `Bureaucrat` de l’exercice précédent.
2. Renommer `Form` → `AForm`, le rendre abstrait, ajouter la fonction `execute`.
3. Créer les trois classes dérivées et leurs comportements.
4. Ajouter `executeForm` à `Bureaucrat`.
5. Écrire les tests dans `main.cpp`.
6. Compiler et corriger les erreurs.
7. Tester toutes les exceptions et tous les cas limites.

---

Souhaites-tu que je te fasse un **tableau de suivi de progression** (genre “étape 1 à cocher”, “étape 2 à cocher”, etc.) pour t’aider à avancer proprement étape par étape ?
